`ljm_constants.json` is provided from https://github.com/labjack/ljm_constants/blob/master/LabJack/LJM/ljm_constants.json

Run `cargo run --bin generate_tags` to generate the labjack constants in `lib.rs` utilizing the information in `ljm_constants.json`.